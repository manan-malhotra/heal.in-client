import { StyleSheet, Text, View } from "react-native";
import React from "react";
import BlogIndividual from "../../../user/home/blogs/[id]";
const ModeratorIndividualBlogs = () => {
    return <BlogIndividual />;
};

export default ModeratorIndividualBlogs;

const styles = StyleSheet.create({});
