import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Relax from "../../user/home/relax";
const ModeratorRelax = () => {
    return <Relax />;
};

export default ModeratorRelax;

const styles = StyleSheet.create({});
