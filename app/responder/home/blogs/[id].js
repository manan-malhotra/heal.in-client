import { StyleSheet, Text, View } from "react-native";
import React from "react";
import BlogIndividual from "../../../user/home/blogs/[id]";
const ResponderIndividualBlogs = () => {
    return <BlogIndividual />;
};

export default ResponderIndividualBlogs;

const styles = StyleSheet.create({});
